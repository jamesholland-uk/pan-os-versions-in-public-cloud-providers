# Pinned here rather than inline in the workflows so that Renovate can see
# them. The one dependency that has actually broken this project was the one
# Renovate could not manage. Every dependency is pinned so that Renovate raises
# each bump as a PR and a run never picks up a new release unannounced.
boto3==1.43.101
azure-identity==1.25.3
# 38.3.0 briefly broke marketplace image listing in Aug 2026 (Compute API
# 2026-04-01 was not yet registered). The AWS and Azure checkers now dry-run
# on any pull request that changes this file, so a break like that shows up
# as a failed check on the Renovate PR.
azure-mgmt-compute==38.3.0
